// Read while input is available
while (getline(cin, s)) {
	// Work with s
}

// Set number of precision digits for floating point variables
// Place it at the start of the code
cout << fixed << setprecision(5);

// Print with fixed precision points
printf("%.5f", x);
